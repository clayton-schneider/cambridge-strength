---
export interface Props {
  className: string;
}

const { className = "" } = Astro.props;
---

<p
  class={className +
    " hidden md:block absolute z-0 -rotate-90 -translate-y-1/2 -translate-x-1/2 font-anton uppercase text-[190px] text-text-faded"}
>
  <slot />
</p>
